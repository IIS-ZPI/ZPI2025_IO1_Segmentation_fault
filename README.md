# Development Instruction

To run the app, firs install the required dependencies using:

```bash
pip install -r requirements.txt
```

then, to start the application, run:

```bash
streamlit run app.py
```

The application is hot-reloaded, so any changes made in the code are visible directly in the UI.
